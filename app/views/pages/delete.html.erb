<%= link_to("<< Back to List", pages_path(:subject_id => @subject.id)) %>
<div>
	<h2>Delete Page</h2>
	<%= form_for(@page, :url => page_path(@page, :subject_id => @subject.id), :method => 'delete') do |f| %>
	 	<p>Are you sure you want to delete this page?</p>
	 	<%= @page.name %>

	 	<div>
	 		<%= f.submit("Delete Subject") %>
	 	</div>

	<%end%>
</div>